# Trabalho de CG

Instale dependências e execute o projeto:

Windows (PowerShell):

```powershell
python -m pip install -r requirements.txt
python main.py
```

Se preferir instalar apenas o pygame:

```powershell
python -m pip install pygame
```
